meta_test←{
    ⍝ SPECIAL CASES::
    ⍝ NOTE:: examples defines no dfns, so n/a is returned
     ns←#.examples.aoc2015

    ⍝NOTE:: same as previous run, but "ns" is now defined in #
     ⎕ ←test ⍬ 

    
     ⎕ ←#.examples test ⍬   ⍝ test namespaces in ⍺
     ⎕ ←test ns             ⍝ only test namespace ⍵
     ⎕ ←ns test 's01' 's02'
     ⎕ ←ns test 's01'
     ⎕ ←ns test 'notafn'
    'No errors in testing framework'
}